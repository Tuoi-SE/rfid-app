---
phase: 06-connection-pooling
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/prisma/prisma.service.ts
autonomous: true
requirements:
  - POOL-01
  - POOL-02

must_haves:
  truths:
    - "PrismaService uses connectionLimit: 20 (or configured value from DATABASE_URL)"
    - "Connection limit is configurable via DATABASE_URL?connection_limit=N without code changes"
    - "PrismaPg adapter receives pool config with max, idleTimeoutMillis, connectionTimeoutMillis"
  artifacts:
    - path: backend/src/prisma/prisma.service.ts
      provides: PrismaService with tuned pg Pool configuration
      contains: "new PrismaPg"
      min_lines: 30
    - path: backend/src/prisma/prisma.service.spec.ts
      provides: Unit tests for pool configuration parsing
      min_lines: 50
  key_links:
    - from: backend/src/prisma/prisma.service.ts
      to: "@prisma/adapter-pg"
      via: "PrismaPg constructor with pool config"
      pattern: "new PrismaPg.*max:"
    - from: parsePoolSize()
      to: DATABASE_URL
      via: "URL searchParams parsing"
      pattern: "searchParams.get.*connection_limit"
---

<objective>
Tune Prisma connection pool for concurrent scan workloads. Update PrismaService to pass pg Pool config (max: 20, idleTimeoutMillis: 30000, connectionTimeoutMillis: 5000) to PrismaPg adapter, with parsePoolSize() helper that extracts connection_limit from DATABASE_URL or falls back to default 20.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-connection-pooling/06-RESEARCH.md
@.planning/phases/06-connection-pooling/06-CONTEXT.md
@backend/src/prisma/prisma.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PrismaService with pg Pool configuration</name>
  <files>backend/src/prisma/prisma.service.ts</files>
  <read_first>
    - backend/src/prisma/prisma.service.ts (current implementation)
  </read_first>
  <action>
    Update PrismaService constructor to:

    1. Add a private method parsePoolSize(databaseUrl: string): number that:
       - Parses DATABASE_URL using `new URL(databaseUrl)`
       - Extracts `connection_limit` from searchParams
       - Returns parsed integer if valid (> 0), otherwise returns 20 (default per D-02)
       - Catches any URL parsing errors and returns 20

    2. Pass pg Pool config to PrismaPg constructor:
       ```typescript
       const databaseUrl = config.get('DATABASE_URL');
       const poolSize = this.parsePoolSize(databaseUrl);

       const adapter = new PrismaPg({
         connectionString: databaseUrl,
         max: poolSize,                          // POOL-01: 20 connections default
         idleTimeoutMillis: 30_000,             // 30 seconds idle before closing
         connectionTimeoutMillis: 5_000,        // 5 seconds to acquire connection
       });
       super({ adapter });
       ```

    Keep the existing onModuleInit() and onModuleDestroy() methods unchanged.
  </action>
  <verify>
    <automated>grep -c "new PrismaPg" backend/src/prisma/prisma.service.ts && grep -c "parsePoolSize" backend/src/prisma/prisma.service.ts && grep -c "idleTimeoutMillis" backend/src/prisma/prisma.service.ts</automated>
  </verify>
  <done>
    PrismaService accepts pool config with max: 20 (or parsed from URL), idleTimeoutMillis: 30000, connectionTimeoutMillis: 5000. parsePoolSize() helper extracts connection_limit from DATABASE_URL or returns 20.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for pool configuration</name>
  <files>backend/src/prisma/prisma.service.spec.ts</files>
  <read_first>
    - backend/src/prisma/prisma.service.ts (the implementation being tested)
  </read_first>
  <action>
    Create unit test file at backend/src/prisma/prisma.service.spec.ts with Jest tests:

    1. Test parsePoolSize extracts valid connection_limit from URL:
       - Input: "postgresql://user:pass@localhost:5432/db?connection_limit=15"
       - Expected: returns 15

    2. Test parsePoolSize returns 20 for URL without connection_limit:
       - Input: "postgresql://user:pass@localhost:5432/db"
       - Expected: returns 20

    3. Test parsePoolSize returns 20 for invalid connection_limit (non-numeric):
       - Input: "postgresql://user:pass@localhost:5432/db?connection_limit=abc"
       - Expected: returns 20

    4. Test parsePoolSize returns 20 for connection_limit=0:
       - Input: "postgresql://user:pass@localhost:5432/db?connection_limit=0"
       - Expected: returns 20

    5. Test parsePoolSize returns 20 for malformed URL:
       - Input: "not-a-valid-url"
       - Expected: returns 20 (fallback)

    Use Jest describe/it blocks. Mock ConfigService to provide test DATABASE_URL values. Use describe('PrismaService'). Use a simple class extension pattern to test private method.
  </action>
  <verify>
    <automated>npm test --prefix backend -- --testPathPattern=prisma/prisma.service.spec --passWithNoTests 2>&1 | head -20</automated>
  </verify>
  <done>
    Unit tests cover pool size parsing from URL, default fallback to 20, and invalid value handling.
  </done>
</task>

</tasks>

<verification>
1. PrismaService compiles without errors: `npm run build --prefix backend 2>&1 | grep -i error`
2. parsePoolSize method exists: `grep -n "parsePoolSize" backend/src/prisma/prisma.service.ts`
3. PrismaPg receives pool config: `grep -n "new PrismaPg" backend/src/prisma/prisma.service.ts`
4. Unit tests pass: `npm test --prefix backend -- --testPathPattern=prisma/prisma.service.spec`
</verification>

<success_criteria>
- [ ] PrismaService passes max: 20 (or parsed value) to PrismaPg adapter
- [ ] parsePoolSize() extracts connection_limit from DATABASE_URL or defaults to 20
- [ ] PrismaPg receives idleTimeoutMillis: 30000 and connectionTimeoutMillis: 5000
- [ ] Unit tests created for pool configuration parsing
- [ ] Application compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/06-connection-pooling/06-01-SUMMARY.md` using the summary template.
</output>
