---
phase: "14"
plan: "04"
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - web/src/app/(auth)/register/page.tsx
  - web/src/app/(auth)/verify-email/page.tsx
  - web/src/features/auth/api/register.ts
  - web/src/features/auth/components/login-page-client.tsx
autonomous: false
requirements: []
user_setup: []
---

<objective>
Frontend: Create registration page with email verification flow and verify-email success page.
</objective>

Purpose: Completes the registration → email verification → login flow.

Output:
- Registration page: email + username form, POSTs to /api/auth/register
- Verify-email page: accepts token from URL, calls GET /api/auth/verify-email
- Login page: add link to registration page

<context>
@web/src/app/(auth)/forgot-password/page.tsx
@web/src/features/auth/components/login-page-client.tsx
@web/src/features/auth/types.ts

# Existing patterns

Forgot-password page style: Light theme (#F4F7FE background), white card (rounded-[40px], p-14), radial gradient background, primary brand (#4c59a8), lucide-react icons.

Auth API pattern (from login.ts):
```typescript
export const loginAuth = async (data: LoginCredentials): Promise<AuthResponse> => {
  const res = await httpClient<AuthResponse>('/auth/login', { method: 'POST', body: JSON.stringify(data) });
  return res.data ? res.data : res;
};
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Registration API</name>
  <files>web/src/features/auth/api/register.ts</files>
  <action>
Create `web/src/features/auth/api/register.ts`:

```typescript
import { httpClient } from '@/lib/http/client';

export interface RegisterData {
  email: string;
  username: string;
  role?: string;
}

export interface RegisterResponse {
  id: string;
  email: string;
  username: string;
  role: string;
}

export const register = async (data: RegisterData): Promise<RegisterResponse> => {
  const res = await httpClient<RegisterResponse>('/auth/register', {
    method: 'POST',
    body: JSON.stringify(data),
  });
  return res.data ? res.data : res;
};
```
</action>
  <verify>
test -f web/src/features/auth/api/register.ts
</verify>
  <done>Registration API file created with POST /auth/register call.</done>
</task>

<task type="auto">
  <name>Task 2: Create Registration Page</name>
  <files>web/src/app/(auth)/register/page.tsx</files>
  <action>
Create `web/src/app/(auth)/register/page.tsx`. Style to match the existing forgot-password page:

```typescript
'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { Mail, User, ArrowLeft, Loader2, AlertCircle, CheckCircle2 } from 'lucide-react';
import { register } from '@/features/auth/api/register';

export default function RegisterPage() {
  const router = useRouter();
  const [email, setEmail] = useState('');
  const [username, setUsername] = useState('');
  const [isLoading, setIsLoading] = useState(false);
  const [isSuccess, setIsSuccess] = useState(false);
  const [error, setError] = useState('');
  const [errors, setErrors] = useState<{ email?: string; username?: string }>({});

  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setError('');
    const newErrors: { email?: string; username?: string } = {};

    if (!email) {
      newErrors.email = 'Vui lòng nhập email';
    } else if (!emailRegex.test(email)) {
      newErrors.email = 'Địa chỉ Email không hợp lệ';
    }
    if (!username || username.length < 3) {
      newErrors.username = 'Tên đăng nhập phải có ít nhất 3 ký tự';
    }
    if (Object.keys(newErrors).length > 0) {
      setErrors(newErrors);
      return;
    }

    setIsLoading(true);
    try {
      await register({ email, username });
      setIsSuccess(true);
    } catch (err: any) {
      const msg = err?.data?.message || 'Đã có lỗi xảy ra. Vui lòng thử lại.';
      setError(msg);
    } finally {
      setIsLoading(false);
    }
  };

  return (
    <div className="min-h-screen flex flex-col items-center justify-center bg-[#F4F7FE] relative overflow-hidden py-4 px-4">
      <div className="absolute inset-0 overflow-hidden pointer-events-none">
        <div className="absolute top-[-10%] left-[-5%] w-[500px] h-[500px] bg-blue-100/40 rounded-full blur-[100px]" />
        <div className="absolute bottom-[-10%] right-[-5%] w-[600px] h-[600px] bg-indigo-100/40 rounded-full blur-[100px]" />
      </div>

      <div className="relative z-10 w-full max-w-[520px]">
        <div className="bg-white rounded-[40px] p-14 shadow-2xl border border-slate-100">
          <button
            onClick={() => router.push('/login')}
            className="flex items-center gap-3 text-slate-500 hover:text-primary transition-colors mb-10 text-base font-black group"
          >
            <ArrowLeft className="w-5 h-5 transition-transform group-hover:-translate-x-1" />
            Quay lại đăng nhập
          </button>

          <div className="text-center mb-10">
            <h1 className="text-[28px] font-black text-slate-900 mb-3 tracking-tight">
              Đăng ký tài khoản
            </h1>
            <p className="text-slate-500 text-base leading-relaxed font-medium">
              {isSuccess
                ? 'Đăng ký thành công! Vui lòng kiểm tra email để xác thực tài khoản.'
                : 'Tạo tài khoản mới để bắt đầu sử dụng hệ thống.'}
            </p>
          </div>

          {isSuccess ? (
            <div className="flex flex-col items-center justify-center py-6">
              <div className="w-20 h-20 rounded-full bg-emerald-50 flex items-center justify-center mb-6">
                <CheckCircle2 className="w-10 h-10 text-emerald-500" />
              </div>
              <p className="font-black text-slate-900 text-xl mb-2">Chúng tôi đã gửi email xác thực!</p>
              <p className="text-slate-500 text-sm text-center mb-8 leading-relaxed">
                Nhấn vào liên kết trong email để kích hoạt tài khoản, sau đó đăng nhập.
              </p>
              <button
                onClick={() => router.push('/login')}
                className="w-full py-4 bg-primary hover:bg-[#000A5C] text-white font-black rounded-2xl text-base transition-all duration-300 shadow-lg"
              >
                Đăng nhập ngay
              </button>
            </div>
          ) : (
            <form onSubmit={handleSubmit} className="space-y-5">
              {error && (
                <div className="flex items-center gap-3 bg-red-50 text-red-600 px-6 py-4 rounded-2xl text-base font-bold border border-red-100 animate-in fade-in slide-in-from-top-2 duration-300">
                  <AlertCircle className="w-5 h-5 shrink-0" />
                  {error}
                </div>
              )}

              <div>
                <label className="block text-[12px] font-bold text-slate-500 uppercase tracking-widest mb-3">
                  Email
                </label>
                <div className="relative group">
                  <Mail className="absolute left-5 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-400 group-focus-within:text-primary transition-colors" />
                  <input
                    type="email"
                    value={email}
                    onChange={e => setEmail(e.target.value)}
                    placeholder="example@riotex.vn"
                    className={`w-full pl-14 pr-5 py-4 bg-white border ${errors.email ? 'border-[#ff9494]' : 'border-slate-200'} rounded-2xl text-slate-800 placeholder-slate-300 focus:outline-none focus:ring-8 focus:ring-primary/5 focus:border-primary transition-all font-bold text-base`}
                  />
                </div>
                {errors.email && (
                  <p className="mt-1.5 text-red-500 text-sm font-medium px-1">{errors.email}</p>
                )}
              </div>

              <div>
                <label className="block text-[12px] font-bold text-slate-500 uppercase tracking-widest mb-3">
                  Tên đăng nhập
                </label>
                <div className="relative group">
                  <User className="absolute left-5 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-400 group-focus-within:text-primary transition-colors" />
                  <input
                    type="text"
                    value={username}
                    onChange={e => setUsername(e.target.value)}
                    placeholder="Tên đăng nhập của bạn"
                    className={`w-full pl-14 pr-5 py-4 bg-white border ${errors.username ? 'border-[#ff9494]' : 'border-slate-200'} rounded-2xl text-slate-800 placeholder-slate-300 focus:outline-none focus:ring-8 focus:ring-primary/5 focus:border-primary transition-all font-bold text-base`}
                  />
                </div>
                {errors.username && (
                  <p className="mt-1.5 text-red-500 text-sm font-medium px-1">{errors.username}</p>
                )}
              </div>

              <button
                type="submit"
                disabled={isLoading}
                className="w-full py-4.5 bg-primary hover:bg-[#000A5C] text-white font-black rounded-2xl text-base transition-all duration-300 disabled:opacity-70 flex items-center justify-center gap-3 shadow-lg hover:-translate-y-0.5"
              >
                {isLoading ? (
                  <Loader2 className="w-5 h-5 animate-spin" />
                ) : (
                  'Đăng ký'
                )}
              </button>
            </form>
          )}

          <div className="mt-10 pt-8 border-t border-slate-100">
            <p className="text-xs text-slate-400 text-center leading-relaxed">
              Sau khi đăng ký, bạn sẽ nhận được email xác thực. Nhấn vào liên kết trong email để kích hoạt tài khoản.
            </p>
          </div>
        </div>

        <div className="text-center mt-6 text-[10px] font-medium text-slate-400">
          © {new Date().getFullYear()} Inventory Pro. Một sản phẩm của Precision Curator Logistics.
        </div>
      </div>
    </div>
  );
}
```
</action>
  <verify>
test -f web/src/app/\(auth\)/register/page.tsx
</verify>
  <done>Registration page created with email + username form, client-side validation, styled to match existing auth pages.</done>
</task>

<task type="auto">
  <name>Task 3: Create Verify-Email Page</name>
  <files>web/src/app/(auth)/verify-email/page.tsx</files>
  <action>
Create `web/src/app/(auth)/verify-email/page.tsx`. This page is accessed via the link in the verification email:

```typescript
'use client';
import { useState, useEffect, Suspense } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';
import { Loader2, CheckCircle2, AlertCircle } from 'lucide-react';

async function verifyEmailToken(token: string): Promise<void> {
  const res = await fetch(`${process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3000/api'}/auth/verify-email?token=${encodeURIComponent(token)}`, {
    method: 'GET',
    credentials: 'include',
  });
  if (!res.ok) {
    const data = await res.json().catch(() => ({}));
    throw new Error(data?.message || 'Xác thực thất bại');
  }
}

function VerifyEmailContent() {
  const router = useRouter();
  const searchParams = useSearchParams();
  const [status, setStatus] = useState<'loading' | 'success' | 'error'>('loading');
  const [errorMessage, setErrorMessage] = useState('');

  useEffect(() => {
    const token = searchParams.get('token');
    if (!token) {
      setStatus('error');
      setErrorMessage('Liên kết xác thực không hợp lệ. Vui lòng yêu cầu liên kết mới.');
      return;
    }

    verifyEmailToken(token)
      .then(() => setStatus('success'))
      .catch((err: Error) => {
        setStatus('error');
        setErrorMessage(err.message || 'Liên kết xác thực không hợp lệ hoặc đã hết hạn.');
      });
  }, [searchParams]);

  if (status === 'loading') {
    return (
      <div className="min-h-screen flex flex-col items-center justify-center bg-[#F4F7FE]">
        <div className="bg-white rounded-[40px] p-14 shadow-2xl max-w-[520px] text-center">
          <Loader2 className="w-16 h-16 text-primary mx-auto mb-4 animate-spin" />
          <p className="text-slate-500 text-base font-medium">Đang xác thực email...</p>
        </div>
      </div>
    );
  }

  if (status === 'error') {
    return (
      <div className="min-h-screen flex flex-col items-center justify-center bg-[#F4F7FE]">
        <div className="bg-white rounded-[40px] p-14 shadow-2xl max-w-[520px] text-center">
          <AlertCircle className="w-16 h-16 text-red-500 mx-auto mb-4" />
          <h2 className="text-2xl font-black text-slate-900 mb-3">Xác thực thất bại</h2>
          <p className="text-slate-500 mb-8 leading-relaxed">{errorMessage}</p>
          <div className="space-y-3">
            <button
              onClick={() => router.push('/login')}
              className="w-full py-4 bg-slate-100 hover:bg-slate-200 text-slate-800 font-black rounded-2xl transition-colors"
            >
              Quay lại đăng nhập
            </button>
            <button
              onClick={() => router.push('/forgot-password')}
              className="w-full py-4 bg-primary hover:bg-[#000A5C] text-white font-black rounded-2xl transition-colors"
            >
              Yêu cầu liên kết mới
            </button>
          </div>
        </div>
      </div>
    );
  }

  // Success
  return (
    <div className="min-h-screen flex flex-col items-center justify-center bg-[#F4F7FE] relative overflow-hidden">
      <div className="absolute inset-0 overflow-hidden pointer-events-none">
        <div className="absolute top-[-10%] left-[-5%] w-[500px] h-[500px] bg-blue-100/40 rounded-full blur-[100px]" />
        <div className="absolute bottom-[-10%] right-[-5%] w-[600px] h-[600px] bg-indigo-100/40 rounded-full blur-[100px]" />
      </div>
      <div className="relative z-10 w-full max-w-[520px]">
        <div className="bg-white rounded-[40px] p-14 shadow-2xl border border-slate-100 text-center">
          <div className="w-20 h-20 rounded-full bg-emerald-50 flex items-center justify-center mx-auto mb-6">
            <CheckCircle2 className="w-12 h-12 text-emerald-500" />
          </div>
          <h1 className="text-[28px] font-black text-slate-900 mb-3 tracking-tight">
            Xác thực email thành công!
          </h1>
          <p className="text-slate-500 text-base leading-relaxed font-medium mb-10">
            Tài khoản của bạn đã được kích hoạt. Bây giờ bạn có thể đăng nhập với email và mật khẩu đã được gửi trong email đăng ký.
          </p>
          <button
            onClick={() => router.push('/login')}
            className="w-full py-4.5 bg-primary hover:bg-[#000A5C] text-white font-black rounded-2xl text-lg transition-all duration-300 shadow-lg hover:-translate-y-0.5"
          >
            Đăng nhập ngay
          </button>
        </div>
        <div className="text-center mt-6 text-[10px] font-medium text-slate-400">
          © {new Date().getFullYear()} Inventory Pro.
        </div>
      </div>
    </div>
  );
}

export default function VerifyEmailPage() {
  return (
    <Suspense fallback={
      <div className="min-h-screen flex items-center justify-center bg-[#F4F7FE]">
        <Loader2 className="w-8 h-8 animate-spin text-primary" />
      </div>
    }>
      <VerifyEmailContent />
    </Suspense>
  );
}
```
</action>
  <verify>
test -f web/src/app/\(auth\)/verify-email/page.tsx
</verify>
  <done>Verify-email page created. Reads token from URL, calls GET /api/auth/verify-email, shows loading/success/error states with styled UI.</done>
</task>

<task type="auto">
  <name>Task 4: Add Registration Link to Login Page</name>
  <files>web/src/features/auth/components/login-page-client.tsx</files>
  <action>
Read the existing login page. Add a registration link at the bottom of the card (below the "Hệ thống bảo mật" footer div). The link should say "Chưa có tài khoản? Đăng ký ngay":

Add after the card footer div (line ~210):

```tsx
<div className="mt-4 pt-4 border-t border-slate-100 flex justify-center">
  <span className="text-slate-400 text-xs font-medium">Chưa có tài khoản? </span>
  <button
    onClick={() => router.push('/register')}
    className="text-xs font-bold text-primary hover:text-blue-700 transition-colors ml-1"
  >
    Đăng ký ngay
  </button>
</div>
```

Make sure `router` is imported (it already is at line 3).
</action>
  <verify>
grep -q 'Đăng ký ngay' web/src/features/auth/components/login-page-client.tsx
</verify>
  <done>Login page has registration link added below the security footer. Navigation to /register works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Registration form data (email, username) in request body |
| Email link → Verify page | Token in URL (GET parameter) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-14-16 | Spoofing | Registration with existing email | mitigate | Backend returns 409 with error message; frontend shows error |
| T-14-17 | Spoofing | Verification token in URL | mitigate | Token is SHA-256 hash (256-bit), URL param read-only; 24h expiry |
| T-14-18 | Denial of Service | Register page brute-force | mitigate | Backend throttle: 5/min; client-side validation reduces unnecessary calls |
</threat_model>

<verification>
1. `cd web && npm run lint 2>&1 | grep -E 'error|Error' | grep -v 'Warning' | head -10`
2. `cd web && npm run build 2>&1 | grep -E 'error|Error|compiled' | head -10`
3. Verify pages exist: `ls web/src/app/\(auth\)/`
4. Verify registration link: `grep 'register' web/src/features/auth/components/login-page-client.tsx`
</verification>

<success_criteria>
- Registration page accessible at /register, styled consistently
- Registration form validates email format and username length client-side
- On success: shows "Đã gửi email xác thực" with success icon
- On email conflict: shows error message from backend
- Verify-email page at /verify-email reads token from URL query param
- Verify-email shows loading → success/error states
- Login page has "Đăng ký ngay" link at bottom
- All pages styled consistently with auth design (brand colors, rounded cards, light theme)
</success_criteria>

<output>
After completion, create `.planning/phases/email-auth/14-04-SUMMARY.md`
</output>
