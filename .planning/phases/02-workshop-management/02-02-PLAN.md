---
phase: "02-workshop-management"
plan: "02-02"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "backend/src/locations/locations.module.ts"
  - "backend/src/locations/locations.service.ts"
  - "backend/src/locations/locations.controller.ts"
  - "backend/src/locations/dto/create-location.dto.ts"
  - "backend/src/locations/dto/update-location.dto.ts"
  - "backend/src/locations/dto/query-locations.dto.ts"
  - "backend/src/app.module.ts"
autonomous: true
requirements:
  - "WORKSHOP-01"
must_haves:
  truths:
    - "Location CRUD API exists for all locations"
    - "Workshop filter (type=WORKSHOP) returns only workshops"
    - "Only name and address are editable (type is fixed after creation - D-03)"
    - "Cannot delete location that has tags (D-04)"
  artifacts:
    - path: "backend/src/locations/locations.module.ts"
      contains: "LocationsModule"
      provides: "Module for Location CRUD"
    - path: "backend/src/locations/locations.service.ts"
      contains: "findAll("
      provides: "List locations with type filter"
    - path: "backend/src/locations/locations.service.ts"
      contains: "create("
      provides: "Create location"
    - path: "backend/src/locations/locations.service.ts"
      contains: "update("
      provides: "Update location name/address only"
    - path: "backend/src/locations/locations.service.ts"
      contains: "remove("
      provides: "Soft delete location (only if no tags)"
---

<objective>
Tạo Location CRUD API với filter type=WORKSHOP (per D-06)

Purpose: Quản lý danh sách xưởng may - CRUD trên Location model với filter type=WORKSHOP. Đây là Workshop CRUD theo WORKSHOP-01.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@backend/prisma/schema.prisma
@backend/src/categories/categories.service.ts
</context>

<interfaces>
From backend/prisma/schema.prisma (existing Location model):
```prisma
model Location {
  id        String       @id @default(uuid())
  code      String       @unique
  name      String
  type      LocationType
  address   String?
  deletedAt DateTime?
  createdAt DateTime     @default(now())
  updatedAt DateTime     @updatedAt
  tags      Tag[]

  @@index([type])
  @@index([code])
  @@index([deletedAt])
}

enum LocationType {
  ADMIN
  WORKSHOP
  WAREHOUSE
  CUSTOMER
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tạo LocationsModule với service và controller</name>
  <files>backend/src/locations/locations.module.ts</files>
  <action>
Tạo thư mục backend/src/locations/ với các file:

**backend/src/locations/locations.module.ts:**
```typescript
import { Module } from '@nestjs/common';
import { LocationsService } from './locations.service';
import { LocationsController } from './locations.controller';
import { PrismaModule } from '../prisma/prisma.module';

@Module({
  imports: [PrismaModule],
  controllers: [LocationsController],
  providers: [LocationsService],
  exports: [LocationsService],
})
export class LocationsModule {}
```

Đăng ký LocationsModule trong backend/src/app.module.ts:
```typescript
import { LocationsModule } from './locations/locations.module';
// ... imports ...

@Module({
  imports: [
    // ... existing imports ...
    TransfersModule,
    LocationsModule,
  ],
  // ...
})
export class AppModule {}
```
</action>
  <verify>
    <automated>ls -la backend/src/locations/ && grep "LocationsModule" backend/src/app.module.ts</automated>
  </verify>
  <done>LocationsModule tồn tại và được đăng ký trong AppModule</done>
  <read_first>backend/src/app.module.ts</read_first>
  <acceptance_criteria>
    - File backend/src/locations/locations.module.ts tồn tại
    - Module được đăng ký trong app.module.ts imports array
    - Module imports PrismaModule
    - Module exports LocationsService
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement LocationsService với CRUD và type filter</name>
  <files>backend/src/locations/locations.service.ts</files>
  <action>
Tạo backend/src/locations/locations.service.ts:

```typescript
import { Injectable, NotFoundException, BadRequestException } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';
import { CreateLocationDto } from './dto/create-location.dto';
import { UpdateLocationDto } from './dto/update-location.dto';
import { QueryLocationsDto } from './dto/query-locations.dto';
import { LocationType } from '@prisma/client';

@Injectable()
export class LocationsService {
  constructor(private prisma: PrismaService) {}

  async findAll(query: QueryLocationsDto) {
    const { page = 1, limit = 20, search, type } = query;
    const skip = (page - 1) * limit;

    const where: any = {
      deletedAt: null,  // Only non-deleted
    };

    if (search) {
      where.OR = [
        { name: { contains: search, mode: 'insensitive' } },
        { code: { contains: search, mode: 'insensitive' } },
        { address: { contains: search, mode: 'insensitive' } },
      ];
    }

    // Key filter: type=WORKSHOP for workshop management (D-06)
    if (type) {
      where.type = type;
    }

    const [data, total] = await Promise.all([
      this.prisma.location.findMany({
        where,
        orderBy: { createdAt: 'desc' },
        skip,
        take: limit,
        include: { _count: { select: { tags: true } } },
      }),
      this.prisma.location.count({ where }),
    ]);

    return { data, total, page, limit, totalPages: Math.ceil(total / limit) };
  }

  async findOne(id: string) {
    const location = await this.prisma.location.findUnique({
      where: { id },
      include: { _count: { select: { tags: true } } },
    });
    if (!location) throw new NotFoundException(`Không tìm thấy vị trí với ID "${id}"`);
    return location;
  }

  async create(dto: CreateLocationDto) {
    // Check code uniqueness
    const existing = await this.prisma.location.findUnique({ where: { code: dto.code } });
    if (existing) throw new BadRequestException(`Mã vị trí "${dto.code}" đã tồn tại`);

    return this.prisma.location.create({
      data: dto,
      include: { _count: { select: { tags: true } } },
    });
  }

  async update(id: string, dto: UpdateLocationDto) {
    const location = await this.prisma.location.findUnique({ where: { id } });
    if (!location) throw new NotFoundException(`Không tìm thấy vị trí với ID "${id}"`);

    // D-03: Only name and address are editable, type is fixed
    // dto should only contain name and address
    if (dto.code) {
      throw new BadRequestException('Không thể thay đổi mã vị trí');
    }
    if (dto.type) {
      throw new BadRequestException('Không thể thay đổi loại vị trí');
    }

    return this.prisma.location.update({
      where: { id },
      data: dto,
      include: { _count: { select: { tags: true } } },
    });
  }

  async remove(id: string) {
    const location = await this.prisma.location.findUnique({
      where: { id },
      include: { _count: { select: { tags: true } } },
    });
    if (!location) throw new NotFoundException(`Không tìm thấy vị trí với ID "${id}"`);

    // D-04: Cannot delete location that has tags
    if (location._count.tags > 0) {
      throw new BadRequestException(
        `Không thể xóa vị trí đang có ${location._count.tags} tags`,
      );
    }

    // Soft delete instead of hard delete
    await this.prisma.location.update({
      where: { id },
      data: { deletedAt: new Date() },
    });

    return { success: true };
  }
}
```
</action>
  <verify>
    <automated>grep -n "async findAll\|async findOne\|async create\|async update\|async remove" backend/src/locations/locations.service.ts</automated>
  </verify>
  <done>LocationsService có findAll (với type filter), findOne, create, update, remove</done>
  <read_first>backend/src/categories/categories.service.ts</read_first>
  <acceptance_criteria>
    - Service có async findAll() với pagination và filters (search, type)
    - Service có async findOne() với _count tags
    - Service có async create() với code uniqueness check
    - Service có async update() - chỉ cho phép update name và address (D-03)
    - Service có async remove() - soft delete nếu không có tags, throw BadRequestException nếu có tags (D-04)
    - findAll filter mặc định chỉ lấy non-deleted (deletedAt: null)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Tạo DTOs và Controller cho Locations</name>
  <files>backend/src/locations/dto/create-location.dto.ts, backend/src/locations/dto/update-location.dto.ts, backend/src/locations/dto/query-locations.dto.ts, backend/src/locations/locations.controller.ts</files>
  <action>
Tạo thư mục backend/src/locations/dto/ và các file DTO:

**backend/src/locations/dto/create-location.dto.ts:**
```typescript
import { IsString, IsNotEmpty, IsOptional, IsEnum } from 'class-validator';

export class CreateLocationDto {
  @IsString()
  @IsNotEmpty()
  code: string;  // e.g., "WS-C", "WH-HN-02"

  @IsString()
  @IsNotEmpty()
  name: string;  // e.g., "Xưởng May C"

  @IsEnum(['ADMIN', 'WORKSHOP', 'WAREHOUSE', 'CUSTOMER'])
  type: 'ADMIN' | 'WORKSHOP' | 'WAREHOUSE' | 'CUSTOMER';

  @IsString()
  @IsOptional()
  address?: string;
}
```

**backend/src/locations/dto/update-location.dto.ts:**
```typescript
import { IsString, IsOptional } from 'class-validator';

// D-03: Only name and address are editable, type is fixed after creation
export class UpdateLocationDto {
  @IsString()
  @IsOptional()
  name?: string;

  @IsString()
  @IsOptional()
  address?: string;

  // Explicitly exclude type and code - they cannot be changed
}
```

**backend/src/locations/dto/query-locations.dto.ts:**
```typescript
import { IsOptional, IsEnum, IsInt, Min, IsString } from 'class-validator';
import { Transform } from 'class-transformer';

export class QueryLocationsDto {
  @IsOptional()
  @IsInt()
  @Min(1)
  @Transform(({ value }) => parseInt(value))
  page?: number = 1;

  @IsOptional()
  @IsInt()
  @Min(1)
  @Transform(({ value }) => parseInt(value))
  limit?: number = 20;

  @IsOptional()
  @IsString()
  search?: string;

  @IsOptional()
  @IsEnum(['ADMIN', 'WORKSHOP', 'WAREHOUSE', 'CUSTOMER'])
  type?: string;
}
```

**backend/src/locations/locations.controller.ts:**
```typescript
import { Controller, Get, Post, Put, Delete, Param, Body, Query, UseGuards } from '@nestjs/common';
import { LocationsService } from './locations.service';
import { CreateLocationDto } from './dto/create-location.dto';
import { UpdateLocationDto } from './dto/update-location.dto';
import { QueryLocationsDto } from './dto/query-locations.dto';
import { JwtAuthGuard } from '../auth/jwt-auth.guard';
import { RolesGuard } from '../casl/roles.guard';
import { Roles } from '../casl/roles.decorator';
import { Role } from '@prisma/client';

@Controller('locations')
@UseGuards(JwtAuthGuard, RolesGuard)
export class LocationsController {
  constructor(private readonly locationsService: LocationsService) {}

  @Get()
  @Roles(Role.ADMIN, Role.WAREHOUSE_MANAGER)
  findAll(@Query() query: QueryLocationsDto) {
    return this.locationsService.findAll(query);
  }

  @Get(':id')
  @Roles(Role.ADMIN, Role.WAREHOUSE_MANAGER)
  findOne(@Param('id') id: string) {
    return this.locationsService.findOne(id);
  }

  @Post()
  @Roles(Role.ADMIN)
  create(@Body() dto: CreateLocationDto) {
    return this.locationsService.create(dto);
  }

  @Put(':id')
  @Roles(Role.ADMIN)
  update(@Param('id') id: string, @Body() dto: UpdateLocationDto) {
    return this.locationsService.update(id, dto);
  }

  @Delete(':id')
  @Roles(Role.ADMIN)
  remove(@Param('id') id: string) {
    return this.locationsService.remove(id);
  }
}
```
</action>
  <verify>
    <automated>ls backend/src/locations/dto/ && grep -l "create-location.dto\|update-location.dto\|query-locations.dto" backend/src/locations/dto/*.ts</automated>
  </verify>
  <done>Locations DTOs và Controller tồn tại với endpoints: GET /locations, GET /locations/:id, POST /locations (ADMIN), PUT /locations/:id (ADMIN), DELETE /locations/:id (ADMIN)</done>
  <read_first>backend/src/categories/categories.service.ts</read_first>
  <acceptance_criteria>
    - create-location.dto.ts có code, name, type (enum), address (optional)
    - update-location.dto.ts chỉ có name và address (không có code và type - D-03)
    - query-locations.dto.ts có page, limit, search, type filters
    - Controller có @Get() findAll với @Roles cho cả ADMIN và WAREHOUSE_MANAGER
    - Controller có @Get(':id') findOne
    - Controller có @Post() create với @Roles(Role.ADMIN)
    - Controller có @Put(':id') update với @Roles(Role.ADMIN)
    - Controller có @Delete(':id') remove với @Roles(Role.ADMIN)
    - Controller dùng JwtAuthGuard và RolesGuard
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Chạy `npx prisma validate` để validate schema
- API test: GET /api/locations?type=WORKSHOP trả về chỉ workshops
- API test: POST /api/locations tạo location mới
- API test: PUT /api/locations/:id với body {name: "new name"} cập nhật thành công
- API test: PUT /api/locations/:id với body {type: "WAREHOUSE"} throw BadRequestException (D-03)
- API test: DELETE /api/locations/:id với location có tags throw BadRequestException (D-04)
</verification>

<success_criteria>
- LocationsModule tồn tại với CRUD operations
- GET /api/locations?type=WORKSHOP trả về chỉ xưởng may (D-06)
- POST /api/locations tạo location mới (ADMIN only)
- PUT /api/locations/:id chỉ update được name và address, không update được type (D-03)
- DELETE /api/locations/:id soft delete được nếu không có tags, throw error nếu có tags (D-04)
- WORKSHOP-01: Quản lý danh sách xưởng may được implement
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/02-workshop-management/02-SUMMARY.md`
</output>
