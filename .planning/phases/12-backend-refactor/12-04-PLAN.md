---
phase: 12-backend-refactor
plan: '04'
type: execute
wave: 2
depends_on:
  - '12-01'
files_modified:
  - backend/src/casl/casl-ability.factory.ts.bak
autonomous: true
requirements: []

must_haves:
  truths:
    - "Stale .bak file da bi xoa khoi codebase"
    - "Khong con file nao duoi .bak extension trong backend/src/"
  artifacts:
    - path: "backend/src/casl/casl-ability.factory.ts.bak"
      provides: "File bi xoa - khong con ton tai"
      deleted: true
  key_links: []
---

<objective>
Xoa stale backup file (D-07) - cleanup task cuoi cung cua Phase 12.

File `.bak` la artifact thua vi git history duoc su dung de recover neu can.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Tu 12-CONTEXT.md:
- D-07: Xoa stale `backend/src/casl/casl-ability.factory.ts.bak`

Tu backend-structure-review.md:
- Issue #5: Stale `.bak` file = LOW priority cleanup
- File `.bak` nen xoa — git history du de recover neu can

Tu backend/src/casl/ directory:
- File: casl-ability.factory.ts (active)
- File: casl-ability.factory.ts.bak (stale - can xoa)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete stale .bak file</name>
  <files>backend/src/casl/casl-ability.factory.ts.bak</files>
  <read_first>
    - backend/src/casl/casl-ability.factory.ts.bak (xac nhan file ton tai)
  </read_first>
  <acceptance_criteria>
    - ls backend/src/casl/casl-ability.factory.ts.bak (file khong con ton tai)
    - ls backend/src/casl/*.bak (khong co .bak file nao trong thu muc)
  </acceptance_criteria>
  <action>
Xoa file bang cach su dung git rm hoac rm thong thuong:

```bash
rm backend/src/casl/casl-ability.factory.ts.bak
```

Hoac neu muon stage cho git:

```bash
git rm backend/src/casl/casl-ability.factory.ts.bak
```

Luu y: Khong can backup vi git history chua noi dung cu.
</action>
  <verify>
    <automated>ls backend/src/casl/*.bak 2>&1 | grep -c "No such file"</automated>
  </verify>
  <done>Stale .bak file da bi xoa</done>
</task>

</tasks>

<verification>
1. Kiem tra khong con .bak file trong backend/src/
2. Chay `cd backend && npm run build` de dam bao build van hoat dong
</verification>

<success_criteria>
1. Khong con file .bak trong backend/src/
2. npm run build thanh cong
</success_criteria>

<output>
Sau khi hoan thanh, tao `.planning/phases/12-backend-refactor/12-04-SUMMARY.md`
</output>
